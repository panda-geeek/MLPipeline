name: test workflow
on:
 push:
  branches: [main]
 workflow_dispatch:
 
jobs:
 test_job:
   runs-on: ubuntu-latest
   
   steps:
     - uses: actions/checkout@v3
     - name: print message
       run: echo "Print Hello"
       
       
